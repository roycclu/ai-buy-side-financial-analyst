# Buy-Side Financial Analyst — environment config template
# Copy to .env and fill in your values.

# ── LLM provider ──────────────────────────────────────────────────────────────
# "anthropic" → Anthropic Claude (supports extended thinking)
# "openai"    → OpenAI, Gemini, or any OpenAI-compatible local server
LLM_PROVIDER=anthropic

# ── Anthropic ─────────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=your_anthropic_key_here
# ANTHROPIC_MODEL=claude-opus-4-6        # default; override if needed

# ── OpenAI / OpenAI-compatible ────────────────────────────────────────────────
# Uncomment and set ONE of the following base URL presets:

# OpenAI
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_API_KEY=your_openai_key_here
# OPENAI_MODEL=gpt-4o

# Google Gemini (OpenAI-compatible endpoint)
# OPENAI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai
# OPENAI_API_KEY=your_gemini_key_here
# OPENAI_MODEL=gemini-2.0-flash

# Local server (vLLM, LM Studio, or any OpenAI-compatible endpoint)
# OPENAI_BASE_URL=http://127.0.0.1:17777/v1
# OPENAI_API_KEY=local
# OPENAI_MODEL=llama3.2:3b

# ── SEC EDGAR ─────────────────────────────────────────────────────────────────
# SEC_EDGAR_USER_AGENT=Your Name your@email.com
